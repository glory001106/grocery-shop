import React from 'react';

const SearchAlert = () => {
    return (
        <div id='search-bar-alert' className='py-10 text-center font-semibold text-red-500' style={{ display: "none" }}>
        </div>
    );
};

export default SearchAlert;